# AWSでの分散テスト

README.mdに記載されている通り、このリポジトリではローカル環境にdocker composeを利用して複数のinfinispanノードを立ち上げて、infinispanへのデータ登録とデータの取得をクラスターを構成するノードに対して行っている。これをAWS環境で、下記の構成で試験する。

* ALB
* ECS(infinispanクラスター) + embededキャッシュのinfinispanアプリケーション
* S3 Ping(infinispanの機能)

より具体的には：

* ALBでECSのタスクグループにラウンドロビンでアクセスを振り分ける。
* キャッシュへの読み書きをすべてラウンドロビンで行う。
* キャッシュは複数のキャッシュをdistribution cacheで構成する。
* ECSのタスク数は各キャッシュのownerよりも多くする。例えばキャッシュA, B, Cがある場合、タスク数が4であれば、いずれのキャッシュもowner数を2にするなど。
* それぞれのキャッシュへの新規追加を十分な回数行い、その後読み出しを十分な回数行い、いずれの値も期待した通りの値になっている。
* それぞれのキャッシュへの更新処理を十分な回数行い、その後読み出しを十分な回数行い、いずれの値も期待した通りの値になっている。（古い値が他のノードから一時的に読み取れることは許容する。）
* キャッシュの読み書きは JSON を用いたAPIで行い、アプリケーションは新規追加/読み出し/更新のいずれもログを出力し、CloudWatchに出力する。

## JSON API

```
データ取得：
GET /api/get/<CACHE_NAME>/<key>
返ってくるデータは任意のJSON形式（APIサーバーからapplication/jsonで受信する。）

データの新規追加/更新：
POST /api/get/<CACHE_NAME>/<key>

ペイロードは任意のJSON（APIサーバーにapplication/jsonで送信する。）
```

## 必要なAWSのスタック

* ECR
* ECS(クラスター、サービス、タスク定義)
* ALB

デプロイは手動で行うので、ローカルの環境としてはアプリケーションのビルドとdockerイメージのビルドができればよい。

